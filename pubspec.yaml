name: orgro
description: An Org file viewer
version: 1.0.0+1

environment:
  sdk: ">=2.3.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  org_flutter:
#    path: ../org_flutter
     git:
       url: https://github.com/amake/org_flutter.git
  google_fonts: ^1.0.0
  url_launcher: ^5.4.1
  file_picker: ^1.4.2
  http: ^0.12.0+4

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
