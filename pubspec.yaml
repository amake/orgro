name: orgro
description: An Org file viewer
version: 1.13.1+38
homepage: https://github.com/amake/orgro

environment:
  sdk: ">=2.6.0 <3.0.0"

dependencies:
  dynamic_fonts: ^1.1.0
  # dynamic_fonts:
  #   path: ../dynamic-fonts-flutter
  file_picker_writable: ^1.1.1+4
  # file_picker_writable:
  #   path: ../file_picker_writable
    # git:
    #   url: https://github.com/amake/file_picker_writable.git
    #   ref: android-file-not-found
  flutter:
    sdk: flutter
  flutter_charset_detector: ^0.3.0
  # flutter_charset_detector:
  #   path: ../flutter_charset_detector
  google_fonts: ^1.0.0
  http: ^0.12.0+4
  intl: ^0.17.0
  native_state: ^1.1.0
  org_flutter: ^0.9.0
  # org_flutter:
  #   path: ../org_flutter
  shared_preferences: '>=0.5.7+3 <2.0.0'
  url_launcher: ^5.4.1

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/orgro-manual.org
    - assets/fonts/
